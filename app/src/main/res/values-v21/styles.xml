<resources>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ksu_yellow</item>
        <item name="colorPrimaryDark">@color/ksu_black</item>
        <item name="colorAccent">@color/ksu_yellow</item>
        <item name="android:statusBarColor">@color/ksu_black</item>
    </style>
    <style name="MyDialogTheme" parent="Theme.AppCompat.Dialog">
        <!-- Fill the screen -->
        <item name="android:layout_width">fill_parent</item>
        <item name="android:layout_height">fill_parent</item>


        <!-- No backgrounds, titles or window float -->
        <item name="android:windowBackground">@null</item>
        <item name="android:windowNoTitle">true</item>


        <!-- Just to prove it's working -->

    </style>
</resources>
